[project]
name = "ai-data-pipeline-rca-agent"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "python-dotenv",
    "pydantic",
    "pyodbc",
    "claude-agent-sdk",
]

[project.optional-dependencies]
dev = ["pytest"]

[tool.setuptools]
packages = ["agent", "config"]